#multi threading

#multi tasking
    #1 multi processing   differt process executing simultaneously
    #2 multi threading      same process executing simultaneously

import threading
print(threading.currentThread().getName())